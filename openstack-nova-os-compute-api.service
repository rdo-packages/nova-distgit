[Unit]
Description=OpenStack Nova Compute API Server
After=syslog.target network.target

[Service]
Type=notify
NotifyAccess=all
TimeoutStartSec=0
Restart=always
User=nova
ExecStart=/usr/bin/uwsgi --plugin python3 --ini /etc/nova/api-uwsgi.ini

[Install]
WantedBy=multi-user.target
